[pytest]
pythonpath = .
testpaths = tests
addopts = --strict-markers -v --tb=short --cov=src --cov-report=html
markers =
    slow: marks tests as slow
    integration: marks tests as integration tests
